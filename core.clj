(ns hello-world.core)

(defn -main
  "A simple Hello World function."
  []
  (println "Hello, World!"))

; To run:
;
; - `clj` to start the interpreter, then (hello-world.core/-main).
; - `clj -m hello_world.core`.
